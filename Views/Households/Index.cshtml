@model IEnumerable<PinewoodGrow.Models.Household>

@{
    ViewData["Title"] = "Index";
}
<br />
<h1 style="text-align: center;">Household Information</h1>

<br />
<p>
    <a asp-action="Create" class="btn btn-sm btn-info right float-right">Create Household</a>
</p>
<br />

<div class="table-responsive-lg">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.HouseIncome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LICO)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.HouseIncome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LICO)
                    </td>
                    <td>
                        <div class="btn-group">
                            <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-sm btn-info">Edit</a>
                            <a asp-action="Details" asp-route-id="@item.ID" class="btn btn-sm btn-primary ml-1">Details</a>
                            <a asp-action="Delete" asp-route-id="@item.ID" class="btn btn-sm btn-danger ml-1">Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


@*<table class="table table-striped">
    <thead>
        <tr>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => model.HouseIncome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LICO)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td></td>
            <td>
                @Html.DisplayFor(modelItem => item.HouseIncome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LICO)
            </td>
            <td>
                <div class="btn-group">
                    <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-sm btn-info">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.ID" class="btn btn-sm btn-primary ml-1">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
                </div>    
            </td>
        </tr>
}
    </tbody>
</table>
*@